# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me
POSTGRES_DB=anketa_db

# Backend
# When running via docker-compose, host must be the "db" service name.
DATABASE_URL=postgresql://postgres:change_me@db:5432/anketa_db?schema=public
JWT_SECRET=change_me
